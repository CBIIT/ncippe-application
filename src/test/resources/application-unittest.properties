app.jwt.secret=LBfVTxpYgbepcf62QfkQ_d0oc-1z1nBFLjxb1IUuOi8
spring.datasource.url = jdbc:mysql://local.db.ncippe.publicissapient.tech:3306/biobank?user=biobank&zeroDateTimeBehavior=CONVERT_TO_NULL
spring.datasource.username = biobank
spring.datasource.password = Password001!
patient.report.bucket.name=local-ncippe-bucket
# Stub OAuth2 / JWT values — Spring Boot 2.7 still resolves ${...} placeholders when the test
# context loads OAuth2 resource-server auto-config (e.g. @WebMvcTest); set these so missing
# LOGINGOV_* env vars do not cause IllegalArgumentException during context startup.
LOGINGOV_JWT_JWK_SET_URI=https://localhost/test-jwk-set
LOGINGOV_JWT_ISSUER_URI=https://localhost/